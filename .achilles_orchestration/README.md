# Operations ACHILLES Orchestration

This folder seeds the non-destructive orchestration layer for the Operations repo.

## Role

Operations owns operator-facing workspace pages, calculators, simulators, task panels and control surfaces.

## Rule

Each `/operations/wn` workspace pairs with a `/wn/data` dataspace in the Data repo.

No existing file should be deleted or overwritten during scaffold installation.
